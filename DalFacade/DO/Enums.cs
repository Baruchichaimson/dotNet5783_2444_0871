
namespace DO;

public struct Enums
{
    public enum CoffeeShop { COFFE_MACHINES, CAPSULES, ACCESSORIES, FROTHERS, SWEETS };
    public enum User { EXIT, ADD, DELETE, UPDATE, GET, PRINTALL, BY_ORDER_AND_PRODUCT, ITEM_BY_ORDER_ID };
    public enum UserForMain { EXIT, PRODUCT, ORDER, ORDER_ITEM }
}
